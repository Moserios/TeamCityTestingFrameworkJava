package com.example.teamcity.api.constants;

/**
 * Fixed TeamCity system project identifiers (not test data — these are the same
 * on every TeamCity instance and never generated).
 */
public final class ProjectId {
    public static final String ROOT = "_Root";

    private ProjectId() {
    }
}
